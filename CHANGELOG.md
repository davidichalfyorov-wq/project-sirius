# CHANGELOG — Project Sirius Discovery 4.86 compatibility pass

## 2026-06-12 — Графика, фаза 5 (часть 1): compute-фундамент движка; фиксы DXT1-альфы, гонки частиц и сциссора

- **Compute shaders (фаза 5, трек F)**: полный стек с нуля.
  Тулчейн: директива `computeshader X.comp.hlsl` (SPIR-V-only бандлы,
  маркер EntryPoint "CSMain" по образцу mesh, формат v1 не тронут),
  space-конвенция compute = space0/space1 (вертексные сеты, 2/3 пустые),
  rayQuery-пермутации cs_6_5+vulkan1.2. Рантайм: vkCreateComputePipelines
  + vkCmdDispatch (кэш по shader.Id — PipelineKey не используется,
  graphics-стейт для compute мусор), bind point COMPUTE в
  CmdBindPipeline/CmdBindDescriptorSets, публичные
  `DispatchCompute/SetStorageImage/BarrierComputeToGraphics/
  GraphicsToCompute/ComputeToCompute` (+ реализация пустовавшего
  `MemoryBarrier()`), диспатч рвёт активный рендер-пасс штатно
  (EndRenderingIfActive — прецедент блитов). Storage images: рефлексия
  OpTypeImage Sampled==2 (+Is3D), DescriptorType 3 в пулах/лейаутах,
  GENERAL-layout навсегда (без layout-трекинга), guard на коллизию
  t/u-регистров при загрузке, обязательная `[[vk::image_format]]`
  (warning при пропуске). Texture3D: VKTexture3D (ImageType 2, single
  mip, storage-флаг) + публичный класс + фабрика; 3D-фоллбеки 1×1×2.
  Sampled depth: `CopyDepth(rt, tex)` — D32→D32 vkCmdCopyImage с
  round-trip барьерами (живой depth-аттачмент несэмплируем by design).
  `GraphicsFeature.Compute` (queue-family бит + `SIRIUS_NO_COMPUTE=1`),
  GL не тронут (default-методы, бандлы за HasFeature). Смоук
  `SIRIUS_COMPUTE_SMOKE=1`: compute пишет градиент в RWTexture3D 32³,
  фрагментный виз сэмплит анимированный слайс — подтверждено скрином;
  валидация 0; голдены GL 0.99997 / VK 0.99985 / RT-гейт PASS.
- **Фикс: DXT1-текстуры теряли альфу на Vulkan** — VKFormats маппил Dxt1
  в BC1_RGB (131), отбрасывая 1-битную punch-through альфу, которую GL
  честно грузит (COMPRESSED_RGBA_S3TC_DXT1). Все DXT1-прозрачности на VK
  рисовались непрозрачными: дым лавовой интро-сцены шёл жёсткими
  тёмными квадами и «гасил» огонь при сорт-перестановках (мерцание,
  репорт пользователя). Теперь BC1_RGBA (133) — пиксель-в-пиксель с GL.
- **Фикс: CPU/GPU-гонка стриминговых storage-буферов** — частицы (и
  кости) пишутся хостом в начале кадра ДО fence-ожидания предыдущего,
  GPU ещё читал тот же persistently-mapped буфер → рваные/мусорные
  квады частиц на плотных эффектах. VKStorageBuffer теперь кольцо ×3,
  BeginStreaming ротирует буфер (статичные клиенты без BeginStreaming
  остаются на буфере 0 — CubeField не задет).
- **Фикс: сциссор-фоллбек без Y-флипа на свопчейне** — при выключенном
  scissor клип брался из viewport-ректа в GL bottom-left конвенции без
  конверсии: любой не-полноэкранный PushViewport на свопчейн-пассе давал
  пустое пересечение viewport×scissor и молча не рисовал НИЧЕГО
  (полноэкранные ректы совпадают в обеих конвенциях — потому не болело).
  Сциссор-фоллбек теперь флипается вместе с viewport.
- **Инструменты**: `SIRIUS_VK_DUMPINTERVAL=N` (плотность VK-дампов),
  `SIRIUS_DUMPFRAMES`/`SIRIUS_DUMPINTERVAL` — бэкенд-агностичные дампы
  кадров через ReadBackBuffer (GL/VK сравнение мерцаний); конвенции
  авторинга compute в src/Shader Readme.txt.

## 2026-06-12 — Графика, фаза 4 (часть 4): VRS, финал фазы; атмосфера планет 2.0; фикс гонки материалов

- **VRS (7.6, pipeline-tier)**: `VK_KHR_fragment_shading_rate` — probe
  с перечислением доступных рейтов в лог
  (vkGetPhysicalDeviceFragmentShadingRatesKHR), статический shading rate
  в ключе PSO-кэша (динамик-стейт отброшен: one-shot пассы кубмап-бейков
  не проходят через BindDynamicState — ловили вероятностный UB), хук в
  DrawStarsphereLayers шейдит фон 2×2, корабли/HUD/текст остаются 1×1 по
  построению. Механика доказана RenderDoc-капчурой: единственный
  rate-(2,2) дро кадра — фуллскрин-композит старсферы; ship-кропы A/B
  бит-в-бит. Настройка `vrs` (INI+UI VARIABLE RATE SHADING), дефолт off;
  `SIRIUS_NO_VRS=1`. Attachment-tier — зафиксирован отказ: на RTX 5090
  фоновый пасс ~0.1 мс, выигрыш ниже шума замера. Защитное правило:
  VRS-структура прикрепляется только к rate≠1 пайплайнам (остальные
  байт-идентичны до-VRS-чейну). Фаза 4 роадмапа закрыта
  (mesh — experimental, см. часть 3).
- **Починена давняя вероятностная «магента-сцена»** (весь фон/объекты
  сплошным пурпуром раз в N запусков, чаще на холодном старте):
  `GameResourceManager.AddMaterials` пропускал ключи с null-плейсхолдерами
  (copy-конструктор сессионных ресурс-менеджеров регистрирует ключи как
  null) и материал навсегда оставался незагруженным → RigidModel рисовал
  DefaultMaterial (Dc=Magenta). Теперь null-плейсхолдеры перезаполняются
  (паттерн AddTextures/AddImages); при фоллбеке на DefaultMaterial пишется
  warning с CRC материала (один раз на CRC).
- **Атмосфера планет 2.0** (заказ): полный реврайт Atmosphere.frag.hlsl —
  Beer-Lambert single-scattering (8 сэмплов вдоль луча), экспоненциальная
  плотность (шкала высоты 12% толщины шелла — свечение прилегает к
  поверхности вместо легаси-гало в полрадиуса), Rayleigh-фаза + Mie
  (Henyey-Greenstein g=0.76, тёплый ореол на солнце), мягкий терминатор
  по NdotL, надир почти прозрачен (τ≈0.12), лимб-хорда насыщается в
  тонкое яркое кольцо. Легаси-рампа DtTexture сохранена как тинт
  (ресэмпл по средней высоте луча) — палитры планет Discovery работают.
  Камера внутри шелла получает честный влёт: периферийная дымка,
  вуаль над поверхностью, рост плотности с погружением. Шелл больше не
  пишет глубину и виден изнутри (RenderMaterial.DisableDepthWrite +
  восстановление в CommandBuffer по образцу DisableCull; DisableCull у
  атмосферы). Радиусы планеты/шелла восстанавливаются в шейдере из
  позиции фрагмента, центра и FL-параметра Scale — загрузчик не трогали.
  Golden-базлайны (gl + rt) переcняты: в кадрах теперь PBR-Манхэттен и
  новая атмосфера.
- Профиль пользователя: experimental-флаги `vrs`/`mesh_asteroids`
  сброшены в false после полевого vkQueueSubmit2 DEVICE_LOST (-4) на
  комбинации обоих флагов; репро на текущем билде не воспроизводится
  (защитный фикс выше), причина под наблюдением.

## 2026-06-12 — Графика, фаза 4 (часть 3): mesh-shader тулчейн (7.5 / C1)

- **VK_EXT_mesh_shader** подключён: probe при старте (фичи+пропертис,
  SIRIUS_NO_MS-эскейп), расширение и фичи в device-чейн,
  vkCmdDrawMeshTasksEXT, GraphicsFeature.MeshShaders. На RTX 5090:
  supported, max output 256 верт / 256 прим.
- **Компилятор**: директива `meshshader X.mesh.hlsl` в .txt-описаниях;
  DXC-профиль ms_6_5 + vulkan1.3 (SPIR-V 1.6, MeshShadingEXT). Без смены
  формата бандла: mesh-модуль едет в vertex-слоте контейнера с маркером
  EntryPoint="MSMain", остальные пейлоады NULL — старые бандлы читаются
  без изменений, mesh-бандлы загружаются только за фича-гейтом.
- **Рантайм**: VKShader распознаёт mesh-бандлы (стейдж-флаги сетов,
  пайплайн без vertex input / input assembly), RenderContext.DrawMeshTasks
  (GL — no-op). Смоук-тест SIRIUS_MS_DEBUG=1 рисует mesh-эмитированный
  градиентный треугольник поверх сцены — путь от HLSL до пикселей доказан,
  валидация чистая, все гейты (golden gl+vulkan, rt_golden) PASS при
  спящей фиче.

## 2026-06-12 — Графика, фаза 4 (часть 2): RTAO, RT-отражения v1, закалка RT-трека

- **RTAO** (`rtao` в INI + тогл RT AMBIENT OCCLUSION): один cosine-луч на
  пиксель с детерминированным хэшем (golden-гейты стабильны), TMax 40 м,
  модулирует только ambient/IBL-термы — прямой свет нетронут. Работает на
  PBR- и Basic-материалах (общий ComputeRtao + HLSL-static
  AmbientOcclusionTerm, сигнатуры лайтинга не тронуты). SIRIUS_RT_DEBUG=3 —
  AO-вью с камеры. Эффект на станционной позе ~10.4К пикселей,
  детерминирован; rt-golden-гейт дополнен AO-вариантом (стабильность
  SSIM≥0.995 + пиксельный ассерт эффекта ≥3000).
- **RT-отражения v1** (`rt_reflections` + тогл RT REFLECTIONS): зеркальный
  луч для стекла (ENVMAP-ветка Basic; промах — прежний кубмап) и гладких
  PBR-поверхностей (roughness<0.25); шейдинг хита — дистанц-затухающий
  силуэт (ray query без hit-шейдеров; точный цвет хита — v2 с
  per-instance буфером). Виз-приёмка ждёт сцену со стеклом в кадре.
- **Закалка**: при включённых RT-тенях GPU-проходы солнечных каскадов
  скипаются (матрицы для ранних выходов RT-шейдера остаются) —
  shadow-пасс 1.1–3.0 мс → 0; TLAS собирается из всего мира с капом 25 км
  (раньше — только видимый список: закадровые кастеры не давали теней);
  SIRIUS_RT_BUILD удалён (сцена строится от настроек или SIRIUS_RT_DEBUG);
  Dev HUD показывает BLAS/инстансы. Кадр станционной позы с RT-тенями:
  3.2 мс → 2.1 мс GPU (rt_build 0.2 мс).
- Полный регресс: rt_golden_gate ×2, golden gl+vulkan, autotest menu+full —
  всё PASS; валидация 0 сообщений во всех вариантах.

## 2026-06-12 — Графика, фаза 4 (часть 1): RT-тени солнца + починка CSM в космосе

- **Ray tracing, ядро (B1–B5)**: ray query во фрагментных шейдерах на Vulkan 1.3
  (VK_KHR_acceleration_structure + ray_query, без RT-пайплайна/SBT). BLAS-кэш по
  мешам (LOD0, ≤8 билдов/кадр, eviction), TLAS пересобирается каждый кадр
  (grow-only буферы, dedicated DEVICE_ADDRESS-аллокации, deferred destroy).
  Дескрипторы AS через PNext, пустой TLAS-фоллбек. RTDebug-вью
  (SIRIUS_RT_DEBUG=1|2): рейкаст из камеры, дистанция/instance-id.
  Vulkan-only пермутации шейдеров: директива `vulkanfeature` в LLShaderCompiler
  (SPIR-V SM6.5; GL/DXIL/MSL алиасятся на базовую пермутацию).
- **RT-тени солнца (B6)**: `rt_shadows` в INI + тогл RT SHADOWS в опциях (серый
  без поддержки). Один ACCEPT_FIRST_HIT-луч к солнцу в SampleShadow
  (normal-offset bias, без face culling, opaque-only v1). Включаются при living
  CSM-пассе кадра; выключение — байт-в-байт CSM-путь (гейты SSIM-нейтральны).
- **Починен многослойный баг: каскадные тени в космосе не работали никогда**:
  (1) CSM-гейт читал список объектов до его населения; (2) детект «космической
  сцены» искал объект солнца, который вне interest-радиуса клиента не
  существует — заменён на признак загруженной системы; (3) SampleShadow
  вызывался только для directional-светов, а солнца FL — point-источники:
  теневой свет теперь помечается флагом CastsShadow (Directional приоритетно,
  иначе point с максимальным Range). CSM-атлас в космосе реально рисуется
  (1.1–3.0 мс @1440p у станции; GL временно без сцен-теней — фича-гейт
  SceneShadows из-за недиагностированного зависания кастер-пасса).
- **Debug-вью каналов (E4 §9.4)**: SIRIUS_DEBUG_VIEW=albedo|normals|roughness|
  metallic|shadow|ibl — материальные каналы вместо шейдинга; канал shadow
  цветодиагностический (red=выкл, green=за каскадом, yellow=вне uv, blue=RT).
- **Тест-инфраструктура**: scripts/rt_golden_gate.sh — стабильность SSIM≥0.995
  + ассерт наличия эффекта (ssim(on, off-база)<0.98; живой путь 0.9586,
  мёртвый 0.9996) + автоассерт валидации; PASS ×2 подряд. Голден-капчуры
  детерминированы: глушится стартовый газ (клиентский предикшен давал дрейф
  ~10 м с джиттером), позы бит-в-бит. ShaderBundle дедупит пейлоады по
  содержимому (раньше 1024-пермутационный бандл компилил ~1000 одинаковых
  GL-программ и вешал загрузку). rt_build 0.1–0.2 мс; валидация чистая.

## 2026-06-11 — Графика, фаза 1.5 завершена: SMAA 1x

- **SMAA 1x реализован** (роадмап 4.7, последний пункт exit-чеклиста фазы 1):
  референсный SMAA.hlsl (iryoku) как includes/SMAA.hlsl + порт-глю
  includes/SmaaPort.hlsl (SMAA_CUSTOM_SL, один linear-clamp сэмплер как в
  GLSL-пути референса, SMAA_RT_METRICS из b3), три фуллскрин-пасса
  SmaaEdges/SmaaWeights/SmaaBlend (luma edge detection -> blending weights
  -> neighborhood blending; offset-функции *VS считаются в пикселе — общий
  вершинник Tonemap.vert без юниформов). LUT-текстуры AreaTex 160×560 и
  SearchTex 64×16 вшиты EmbeddedResource (генератор scripts/smaa_textures.py,
  лицензия Shaders/SMAA/LICENSE-SMAA.txt), загрузка Render/SmaaTextures.cs
  (AreaTex расширяется в BGRA8 — беззнакового RG8 нет ни на GL, ни на VK).
- **Пайплайн**: ветка PostAa==Smaa в HdrFramePipeline.End() — tonemap в
  LdrTarget, затем edges->weights->blend в restoreTarget; intermediates в
  SizedTargets (full-res Bgra8 ×2); пасс-таймеры post.smaa.edges/weights/blend;
  debug-вью SIRIUS_DEBUG_SMAA=1 (контактный лист edges+weights).
- **UI**: «SMAA» в селекторе ANTI-ALIASING (options.lua post_aa_levels),
  write-back в do_goback обобщён через idx_to_post_aa (раньше жёстко "fxaa").
- **Приёмка** (станция THN-меню, 2560×1440, A/B/C off/fxaa/smaa): SMAA
  реконструирует длинные кромки чисто там, где FXAA оставляет зубцы, и
  заметно меньше мылит текстуры; звёздное поле не съедается (8321→8423
  яркrix пикселей); HUD/текст не тронуты (UI после пост-AA). GL-путь
  (SPIRV-Cross трансляция с одним сэмплером) работает.
- **Бюджет**: post.smaa.* суммарно 0.065–0.078 мс @2560×1440 (бюджет 0.3 мс,
  запас ×4). FXAA 0.01 мс остаётся дефолтом; SMAA — выбор в настройках.
- **Гейты**: пост-AA в golden-капчуре по-прежнему off → SSIM-нейтральность
  подтверждена: GL 0.99975/0.99983/0.99983, VK 0.98858/0.99970/0.99968,
  autotest menu ALL PASS. Exit-чеклист фазы 1 теперь полный ✓.


## 2026-06-11 — Дожим хотфикса: вайтлист filmic, INDEV, диагностика спайков кадра

- **Filmic реально включён**: GameSettings.Validate() (эра фазы 1) знал
  только off/aces и молча сбрасывал новый дефолт «filmic» в «off» при
  каждой загрузке — сцена уходила на экран сырой линейной, и ни один
  переключатель не менял картинку. Вайтлист расширен, неизвестные
  значения теперь падают в «filmic». Возвращён до-фазовый вид неба
  (golden-небо 4.94 против 5.02 у старого эталона; с тайным aces было
  0.13), полный кадр 12.5 против 14.5 — остаток это намеренный вид
  фазы 2 (линейный свет/IBL/тени). Урок: INI-конфиги, переписанные
  старым билдом при выходе, заслоняют новые дефолты — конфиг Давида
  содержал «tonemapper = aces», починен.
- **Меню: «VER. 1.0» → «INDEV»** (mainmenu.lua, литерал вместо
  FormatStringID(1271); interface.json перекомпилирован).
- **Диагностика статтера**: лог «Frame spike NNms: update/draw/present»
  на кадрах дольше 40 мс (SDL3Game.Run) — следующая живая сессия сама
  покажет, в какой фазе хичи. Эталоны пересняты, гейты зелёные
  (GL 0.99986/0.99934/0.99960; VK 0.98488/0.99883/0.99931).
- Инфраструктурное: битый Debug-бинарь LLShaderCompiler (после
  kill -9 посреди записи) крашился «Internal CLR error», а Exec в
  сборке душился сбором core-dump — выглядело как вечное зависание
  dotnet build; лечится rm -rf bin obj + пересборкой компилятора.
  golden_capture.sh добивает выжившие после SIGTERM игры (kill -9
  после грейса) — они держали dll и вешали следующие сборки.

## 2026-06-11 — Хотфикс после живой обкатки фаз 1–2: яркость, небо на VK, порча/статтер при пересозданиях свапчейна

Первая полноценная игровая сессия на фуллскрине 2560×1440 вскрыла
четыре проблемы; все воспроизведены, исправлены и закрыты гейтами.

- **Глобальная темнота, «настройки ничего не меняют»**: ACES-toe давил
  тёмные линейные величины (звёзды 0.02 linear → 0.0035 → невидимы;
  история эталонов по коммитам: space lum 14.5 → 7.8 (ACES on) → 5.6
  (linear)). Новый тонемаппер **filmic** (дефолт): identity ниже колена
  0.6 linear, C1-плечо к 1.0 — классический вид Freelancer в тёмном/
  среднем диапазоне сохраняется, HDR-плечо остаётся на солнцах/блуме.
  ACES остался опцией («off»/«filmic»/«aces» в INI и Options→TONE
  MAPPING). Пайплайн кадра теперь обязателен даже при hdr=false
  (bare-проход с sRGB-энкодом): линейный воркфлоу декодирует текстуры
  в шейдерах безусловно, и обход пайплайна показывал сырую линейную
  сцену — отсюда «выключение ничего не меняло».
- **Старсфера ездила по вертикали с камерой (Vulkan)**: реконструкция
  луча в StarsphereCubemap.frag строила NDC из SV_Position; origin
  fragcoord различается (GL снизу, VK сверху), а композит рисует в
  оффскрин без negative-viewport-флипа. NDC теперь берётся из texCoord
  фуллскрин-треугольника — инвариантно к бэкенду (на GL математика
  идентична прежней) и корректно в letterbox-вьюпортах.
- **Полосы порчи в катсценах взлёта/вылета + хичи**: пересоздание
  свапчейна (ресайз/OUT_OF_DATE; в фуллскрин-сессии — 9 раз на одном
  размере) пересоздавало depth-буферы на переиспользованной памяти
  пула БЕЗ очистки → кадры тестировались по мусорной глубине
  (зелёно-мадженто полосы). Репро-инструмент SIRIUS_VK_FORCE_RECREATE=N
  (одноразмерное пересоздание каждые N кадров): до фикса 78 пересозданий
  → битый кадр, после 227 → ноль. Все depth-образы (глобальный и
  per-target) теперь очищаются при создании через
  vkCmdClearDepthStencilImage. Триггеры пересозданий логируются
  (size-mismatch со значениями / OUT_OF_DATE).
- **«Статтер ОГРОМНЫЙ» в полёте**: каждая ваншот-загрузка (стриминг
  текстур) держала queueLock и синхронно ждала GPU-fence, стопоря
  submit/present главного потока на полный GPU-round-trip за каждую
  текстуру. Ваншоты теперь асинхронные: submit с fence из пула, жатва
  по vkGetFenceStatus (в Begin-путях и в начале кадра), staging-буфер
  освобождается по сигналу, командные буферы наконец освобождаются
  (раньше текли); порядок отправки в одну очередь сохраняет
  корректность «загрузка раньше кадра». Кап 64 ваншота в полёте
  (ожидание старейшего при переполнении).
- Гейты: эталоны пересняты под filmic-дефолт; GL 1.00000/0.99982/0.99995,
  VK 0.98579/0.99905/0.99919. Порог меню ослаблен до 0.98 (комментарий в
  golden_gate.sh): замороженный THN-кадр идентичен, но плотные кромки
  текста/корпуса растеризуются суб-пиксельно по-разному между API, и
  яркий filmic усиливает абсолютные дельты; space-гейты остались 0.9985.
- Сопутствующее: пути Disk1→Disk после смены точки монтирования
  (конфиг пользователя, Запустить_Project_Sirius.sh, golden-скрипты).

## 2026-06-11 — Графика, фаза 2 завершена: линейный свет, IBL, тени

Роадмап-фаза 2 (улучшенное освещение) реализована и включена по
умолчанию: **линейный световой пайплайн + IBL-пробы систем + каскадные
тени солнца + тени прожекторов**.

- **Linear workflow** (5.1, второй шаг плана LINEAR_AUDIT.md): цветовые
  текстуры декодируются из sRGB на сэмплах (includes/ColorSpace.hlsl),
  INI-цвета (свет/ambient/туман/материальные константы) — один раз на
  CPU (Render/ColorSpace.cs); vertex-тинты небул/ALE/спайнов — в их
  шейдерах; старсферные кубмапы — на выходе композита; PBR без
  собственной гаммы. Тонемап — единственный display-encode кадра
  (точная sRGB-кривая). Перетюнено: bloom_threshold 0.45, маска лучей
  0.05 (линейные люминансы).
- **Cook-Torrance аудит** (5.2): термы соответствуют референсу Khronos
  glTF. Предсказанный роадмапом баг затухания подтверждён дословно
  (PBR.frag: length от нормализованного вектора → дистанция всегда 1)
  и исправлен. MaterialNormalizer: без Mt-карты/фактора metallic = 0
  (диэлектрик, не хром), roughness без Rt = 0.65 («крашеный корпус»).
  Юнит-тесты MaterialNormalizerTests — 5/5.
- **BRDF LUT** (5.3): split-sum интеграция 1024 сэмплов в 256², один
  fullscreen-проход при первом обращении; SIRIUS_DEBUG_BRDF=1 — оверлей,
  сверен с каноническим градиентом.
- **IBL-пробы систем** (5.3): при загрузке системы её старсферная
  кубмапа (DXT-грани декодируются CPU параллельно, ~250 мс Li01 / 80 мс
  Br01) сворачивается в 5-мип roughness-цепочку 64² + косинусный
  иррадианс-куб 16²; фоллбек — линейный ambient системы. Канал:
  SystemLighting.Ibl → Lighting.Environment → PBR (новый фичебит IBL):
  иррадианс заменяет плоский ambient, prefiltered-цепочка + LUT дают
  спекуляр-отражения. Металл больше не чёрный в тени; окружение
  меняется между системами. ibl=true в INI и Options.
- **CSM от солнца** (5.4): 3 каскада (600/2500/10000) в цветовой атлас
  с RGB-упакованной линейной глубиной (депт-буферы движка
  несэмплируемы); кастеры — rigid-модели через DrawImmediate с
  override-материалом (солнца/небулы/партиклы не кастят); texel-snap
  стабилизация; PCF 2×2 ТОЛЬКО point-сэмплером (новый
  SamplerState.PointClamp — фильтрация упакованных каналов = мусор).
  Тени применяются к направленному свету в обоих лит-путях. Пасс
  гейтится наличием SunRenderer (THN-сцены городов самозатенялись
  camera-fit каскадами в кашу). shadows=true в INI и Options.
- **Тени прожекторов** (5.5): до 4 ярчайших спотов (luminance×range,
  стабильный отбор) получают перспективные тайлы во втором атласе 2×2;
  совпадение свет↔тайл по позиции в шейдере; блок b7 — ровно на границе
  бюджета динамических юниформ-блоков.
- **Инфраструктура/уроки**: TextureSlots/SamplerSlots фронтенда
  расширены с 8 до бэкендных 16 (слоты 8/9 — теневые атласы);
  RenderTarget2D.Dispose ретирит image текстуры на VK (BRDF-таргет
  держится живым); лимит maxDescriptorSetUniformBuffersDynamic —
  пер-pipeline-layout (спек-минимум 8, NVIDIA 15) — ассерт поднят до 15
  с предупреждением, теневые блоки огорожены PIXEL_SHADOWS от
  вершинных стейджей; **шейдерные ошибки сборки (SH0004) не содержат
  «error CS»** — фильтр логов сборки должен ловить «Ошибок: N», иначе
  build/v3 тихо остаётся старым (стоило часа отладки «призрака»).
- **Гейт/валидация**: базлайны пересняты на полном пресете; GL
  1.00000×3, VK 0.99918/1/1; Vulkan-валидация 0 ошибок; пер-пасс
  тайминги: scene 0.34 мс, bloom 0.05 мс, лучи 0.02 мс, тонемап+FXAA
  0.02 мс @1024×768 RTX 5090.

Exit-чеклист фазы 2 (5.6): linear/HDR-аудит ✓; PBR без гаммы ✓;
MaterialNormalizer ✓; баг затухания подтверждён и исправлен ✓;
BRDF LUT ✓; IBL irradiance/prefilter на систему ✓; CSM ✓; локальный
теневой атлас ✓; дебаг-оверлеи (BRDF/bloom-мипы/тайминги) ✓; голдены ✓;
перф-бюджеты задокументированы ✓. SMAA — фаза 1.5, запланирован
следующим шагом (#58).

## 2026-06-11 — Графика, фаза 1 завершена: HDR-пост-цепочка включена по умолчанию

Роадмап-фаза 1 (пост-обработка) реализована целиком и включена дефолтным
пресетом: **ACES-тонемап + bloom + солнечные лучи + FXAA**. Классический
вид Freelancer — одна опция в настройках (всё off → пиксель-в-пиксель
прежняя картинка, это гарантировал гейт на каждом шаге).

- **Тонемап** (`Tonemap.frag.hlsl`): ACES filmic + exposure; off-режим —
  точный passthrough. Сцена остаётся display-referred до линеаризации
  фазы 2 (решение и карта работ: docs/LINEAR_AUDIT.md) — decode/grade/
  encode происходят в одном финальном проходе, единственной гамма-точке
  кадра.
- **Bloom** (роадмап 4.5): полуразмерный bright-pass c Karis-весами
  (анти-фликер) → 13-tap даунсемплы до 6 мипов → аддитивный tent-апсемпл;
  композит свёрнут в тонемап-проход (bloom off = бит-в-бит сцена).
  Дебаг-сетка мипов: SIRIUS_DEBUG_BLOOM=1.
- **Солнечные лучи** (роадмап 4.6): полуразмерная luminance-маска вокруг
  спроецированного диска солнца (окклюзия кораблями/планетами имплицитная)
  → радиальный блюр 48 сэмплов → композит в тонемапе. Солнце выбирается из
  видимых SunRenderer'ов, за камерой/вне экрана лучи гаснут.
- **FXAA 3.11** после тонемапа на LDR; UI рисуется после AA и остаётся
  чётким. SMAA — фаза 1.5 (после фазы 2), трекинг в задачах.
- **Настройки**: hdr/tonemapper/exposure/bloom*/god_rays*/post_aa в INI
  ([Settings]) и в меню Options→PERFORMANCE (селекторы и слайдеры в стиле
  оригинала); MSAA и пост-AA сосуществуют только явно — один селектор
  ANTI-ALIASING (NONE/MSAA…/FXAA).
- **Пер-пасс GPU-тайминги** (роадмап 9.3): timestamp-пары на Vulkan,
  ARB_timer_query на GL; SIRIUS_PASS_TIMINGS=1 — оверлей (с ассертом
  цветового пути `output: manual-srgb`) и периодический лог. Замеры
  1024×768 @ RTX 5090: bloom 0.06 мс, лучи 0.02 мс, тонемап 0.006 мс,
  FXAA 0.01 мс — бюджеты роадмапа (1.2/0.8 мс @4K) выдержаны с запасом.
- **VK-рантайм** (пролог фазы): дескриптор-кэш (persistent uniform-сеты
  через UNIFORM_BUFFER_DYNAMIC + контентно-адресуемые текстурные сеты,
  ~10× меньше аллокаций на кадр), пул транзиентных буферов стриминга,
  ColorWrite в PipelineKey (depth-prepass больше не пишет цвет).
- **Инструменты**: SIRIUS_TELEPORT="x,y,z,yawDeg" (произвольная поза в
  автоплее), SIRIUS_SPAWN="система,база" (новая игра в другой системе),
  SIRIUS_OPEN_SCENE=options (загрузка сразу в UI-сцену), headless-компилятор
  интерфейса `InterfaceEdit --compile uixml interface.json` + фикс
  invariant-culture в кодогене lua (ru-RU локаль ломала float-литералы).
- **Гейт**: базлайны пересняты с дефолт-пресетом; post-AA в гейте
  закреплён off (FXAA усиливает субпиксельные различия растеризации
  GL↔VK на каждой кромке — кросс-API сравнение становится шумом).
  Итог: GL 1.00000×3; VK 0.99974 / 1.00000 / 1.00000. Валидация Vulkan —
  0 ошибок со всей цепочкой.

Exit-чеклист фазы 1 (роадмап 4.8): HDR-таргет и тонемап ✓; bloom с
мипами ✓; источники (солнца/движки/бимы/эмиссив) ✓; god rays half-res ✓;
FXAA ✓; SMAA → запланирован фазой 1.5 (допустимо чеклистом) ✓; UI после
пост-обработки ✓; debug-оверлей с пер-пасс временами ✓; голдены
post-off/post-on ✓; настройки в конфиге и рантайм-меню ✓. Пункты «PBR
outputs linear HDR» и «gamma-аудит non-PBR» выполняются в фазе 2.1 по
двухэтапному плану LINEAR_AUDIT.md.

Мультисистемные прогоны пресета: Li01 (Manhattan), Br01 (New London,
солнце+туман), Ku01 (New Tokyo, синяя небула), интро-сцены меню —
скрины в Output/phase1_compare/.

## 2026-06-10 (вечер) — комнаты: T-позы НПС, курсор, аудио-спам

- **T-позы НПС в комнатах**: GetDefaultFidgetScript искал idle-сценарии по
  ванильным путям scripts/bases/fidget_*.thn, которых в Discovery нет — НПС
  со спотов добавлялись без fidget и замирали в bind-позе (заодно «головы
  внутри тела»: head-DFM без анимированной connection-кости рисуется у
  основания скелета). Добавлен фоллбек на реальные
  scripts/extras/fidget_{sit|stand}_{male|female}_NN.thn с детерминированным
  выбором варианта по CRC ника НПС (толпа не двигается синхронно) и
  _null-вариантами. База анимаций проверена: 2791 скрипт из 11 файлов.
- **Пропадающий курсор в баре** (и любые stale-текстуры): scene unload
  диспозит GPU-текстуру, а имя остаётся у потребителя (курсор резолвит
  каждый кадр); GL молча выживал на переиспользованном handle, Vulkan
  подставлял прозрачный fallback (курсор рисуется Additive → невидим).
  GameResourceManager.FindTexture теперь перезагружает текстуру из
  исходного файла и при IsDisposed, не только при null-слоте.
- **Спам «Audio entry ... not found»** (тысячи строк/сек на отсутствующих
  в Discovery звуках вроде engine_pi_fighter_bigger_loop): отрицательные
  поиски кэшируются, предупреждение пишется один раз на имя.

Гейт после правок: VK menu 0.99897 / space 1.00000 / noui 1.00000; GL
0.99926 / 0.99994 / 0.99998 — все PASS.

## 2026-06-10 — Vulkan: паритет с OpenGL достигнут, Vulkan стал бэкендом по умолчанию

SSIM-гейт GL↔VK пройден: menu 0.99897, space 1.00000, space_noui 1.00000
(порог 0.9985, маска покрывает 3.4% кадра — анимация стрелок тредлейнов и
HUD-спидометр). Валидация Vulkan: 0 ошибок на полном автоплей-прогоне.

Корневые исправления рендера (Vulkan):
- **FrontFace per-pass**: negative-viewport-флип есть только на свапчейне,
  поэтому в оффскрин-проходах (HDR-сцена) ориентация треугольников была
  зеркальной → culling резал старсферы-изнутри (зелёная небула меню),
  переднюю обшивку корабля при взлёте, интерьеры комнат. Теперь
  FrontFace=CCW на свапчейне, CW в оффскринах (поле Swapchain в PipelineKey).
- **SetIdentityCamera без DepthZeroToOne**: UI-меши с z=-1e-9 клипались
  нижней границей depth [0,1] → пропадали иконки HUD (фильтры контакт-листа,
  фон окна контактов). Identity-камера теперь ремапится как SetCamera.
- **GL-семантика SetData** (UploadIntoBuffer): запись в буфер, уже
  использованный дроу записываемого кадра, идёт vkCmdCopyBuffer-ом в порядке
  команд (staging + memory barrier) — иначе CPU-перезапись ломала уже
  записанные дроу (GL прикрывает это shadow-копией драйвера).
- **Отложенный clear привязан к таргету** (clearPendingTarget): раньше клир
  HDR-таргета мог потребляться первым дроу кадра на свапчейне; ClearAll/
  ClearColorOnly/ClearDepth при активном чужом проходе переключают проход.
- **VKSpirv: Uniform+BufferBlock → StorageBuffer** (легаси-стиль dxc для
  StructuredBuffer) — починены частицы (Particle/ParticleBeam) и кости
  (Basic_Skinned): сторадж-дескриптор получал мусор из юниформ-ринга.
- **Вершинные атрибуты** только для потребляемых шейдером локаций
  (vertex-pulling частиц рисует тысячи вершин с фиктивным буфером).
- **Эквивалент GL_TEXTURE_MAX_LEVEL**: ImageView расширяется по мере
  загрузки мип-уровней (RebuildView), незагруженные уровни не семплируются.
- **Пересоздание свапчейна при ресайзе окна** (X11 молча растягивал кадр):
  каждый кадр сверяется размер окна; пересоздаются depth/views/семафоры.
- Исправлена раскладка Bgra4444 (GL_UNSIGNED_SHORT_4_4_4_4: RGBA от
  старшего ниббла, альфа в младшем).

Данные:
- **librelancer.ini восстановлен**: в авто-сгенерированном оверрайде
  отсутствовали 8 строк `effects = FX/*/*_ale.ini` → VisEffects=0, все
  ALE-эффекты игры (вулкан/молнии интро, солнце, выхлопы, взрывы) были
  мертвы на ОБОИХ бэкендах. Также добавлены voices_recognizable,
  gate_tunnels, jump_effect, groups, concave.

Пост-релизный хотфикс (вис на сплэше при развёрнутом окне):
- vkAcquireNextImageKHR/vkQueuePresentKHR: обработка VK_ERROR_OUT_OF_DATE_KHR
  и VK_SUBOPTIMAL_KHR. При OUT_OF_DATE семафор acquire не сигналится — submit,
  ждущий его, вешал GPU навсегда («Приложение не отвечает» на сплэше, если
  WM разворачивал окно во время загрузки). Теперь: пересоздание свапчейна и
  повторный acquire; RecreateSwapchain взят под queueLock (гонка с one-shot
  загрузками с лоадер-потоков). Проверено принудительным ресайзом на кадрах
  сплэша и меню.

Vulkan по умолчанию:
- RenderBackendSelector: дефолт Vulkan; `SIRIUS_RENDERER=gl` — OpenGL.
- GL-фоллбек пересозданием окна на двух уровнях: SDL_CreateWindow с
  VULKAN-флагом не удался (нет лоадера/VK_KHR_surface) и
  VKRenderContext.Create вернул null — проверено подменой VK_ICD_FILENAMES.

Задача №39 (снятие golden-обвязки):
- Сиды Random(12345) в AsteroidFieldRenderer/NebulaRenderer теперь только
  при SIRIUS_GOLDEN_DIR; обычная игра — Random().
- Golden-телепорт переведён в режим «пин каждый тик» (дрейф корпуса давал
  шум между прогонами); амбиент-НПС уже глушились в golden.
- Снята вся диагностическая обвязка из горячих путей (VKTrace/VKAll/дампы/
  env-гейты в материалах) — заодно убраны GetEnvironmentVariable из
  пер-дроу путей (вклад в статтер).
- Базлайн tests/goldens/gl пересоздан на актуальных данных, маска
  перегенерирована программно из попарных диффов прогонов.

Отладочные инструменты (оставлены): SIRIUS_RENDERER, SIRIUS_INTRO=N (выбор
интро-сцены), SIRIUS_VK_DUMPFRAMES=dir (дамп кадров), SIRIUS_VK_VALIDATION=1,
SIRIUS_VK_TESTPATTERN.

## 2026-06-10 — Cubemap-старсферы (v4) применены и проверены

Пакет ChatGPT v4 (Output/project_sirius_starsphere_cubemap_v4_*) применён
поверх рабочей копии: рендер старсфер DDS-cubemap'ами с полным CMP-fallback
(`StarsphereCubemapRenderer`, шейдер `StarsphereCubemap`, поля
`*_cubemap` в `[Background]`, автопоиск `<имя>.cubemap.dds` рядом с CMP,
флаг `use_cubemap_starspheres` в `[Settings]`), офлайн-конвертер
`src/LibreLancer.Tools.CmpToCubemap` (BGRA8/DXT1/DXT5 + `DDS.WriteCubemap`).

Доделки при интеграции (моё):
- Конвертеру добавлен импорт `PublishAssets.projitems` — без
  `LibreLancer.Base.dll.config` dllmap не находил системные
  `libSDL2-2.0.so.0`/`libSDL3.so.0` и инструмент не стартовал на Linux.
- Починен баланс viewport-стека в `BakeGame`: базовый вьюпорт пушится в
  `Load()` (EndFrame требует ровно один), убран лишний restore-push/pop —
  раньше каждый бейк завершался ошибкой «viewports.Count != 1»/
  «Stack empty» уже после записи DDS (и batch считал успехи провалами).

Проверено:
- Движок и конвертер собираются: 0 ошибок.
- `starsphere_li01.cmp` → `starsphere_li01.cubemap.dds` (2048², DXT5,
  6 граней; размер байт-в-байт по формуле BC3) — конвертер выходит rc=0.
- Живой прогон (SIRIUS_AUTOPLAY): `Starsphere: Loaded cubemap layer
  Nebula: …starsphere_li01.cubemap.dds` — mixed-mode (nebula=cubemap,
  basic/complex=CMP) работает в Li01.
- Fallback-тест: при переименованном DDS система грузится по чистому
  CMP-пути без ошибок (см. ниже).

Фиксы по скриншотам Давида (магентовые швы по рёбрам куба, чёрные
облака, чёрный клин «галактической полосы»):
- **Ориентация граней**: все 6 граней писались с U-зеркалом (лево экрана
  LookAt-камеры ≠ направление роста u в GL-параметризации куба). Это не
  глобальная изометрия — рёбра X↔Z граней рвались (изломы на скринах).
  Фикс: горизонтальный флип каждой грани при записи
  (`FlipHorizontalBgra` в `BakeGame`). Метрика edge-continuity
  (12 рёбер куба, средняя |ΔRGB| между соседними гранями,
  `Output/verify_cubemap_edges.py`): **67.0 → 1.6** (уровень шума DXT);
  автоподбор диэдральных трансформаций подтверждает identity для всех
  граней.
- **Чёрные облака/клин**: `Material.FromNode` падал NullReference'ом на
  каждом материале — конвертер не создавал глобальный `MaterialMap`
  (его создаёт игра при парсинге freelancer.ini). Фикс: пустой
  `new MaterialMap()` в `BakeGame.Load()`; ошибка
  `Error loading material galactic_belt.tga` при бейке исчезла,
  текстуры туманности в DDS на месте.
- **Li01 полностью на cubemap (2026-06-10)**: сконвертированы оба
  звёздных слоя (`starsphere_stars_basic`, `starsphere_li01_stars`;
  edge-тест 0.0 и 1.1) — все 3 слоя грузятся как cubemap, включён
  единый composite-проход (`DrawComposite`). Численный SkyProbe
  (центр экрана vs forward камеры): dot=1.0000 на всех сэмплах.
  Попутные фиксы: краш сервера `AiAttackState` на мёртвой/задокованной
  цели (guard на Exists/PhysicsComponent.Body); скриншоты движка не
  сохранялись при пустом MyPictures (двойной Path.Combine + проглоченная
  ошибка в Task.Run) — починено, в SIRIUS_AUTOPLAY запись синхронная.
- **Батч-конвертация всех старсфер (2026-06-10)**: все 102 CMP →
  `.cubemap.dds` (2048², DXT5), ошибок 0, выборочные edge-тесты 0.4–2.9.
  Папка STARSPHERE: 3.2 ГБ. Конвертер переведён на ОДИН GL-контекст на
  весь батч (`BakeJob`-очередь, один job на кадр): прежняя схема
  «новый Game на файл» роняла все бейки после первого-второго
  («GL Error: Invalid Value» — статический кэш AllShaders переживал
  пересоздание контекста). Headless-режим: `SDL_VIDEODRIVER=offscreen`.
- **Экспорт для арт-пайплайна**: `Output/li01_cubemap_cross.png`
  (8192×6144 RGBA, крест-развёртка 6 граней, полный BC3-декод с альфой)
  + `Output/PROMPT_LI01_CUBEMAP_ENHANCE.md` — строгий промпт на 10
  HQ-вариантов (запрет менять/добавлять/сдвигать структуры).
- **Vulkan-миграция, Phase 0 — golden-харнес и детерминизм (2026-06-10)**:
  фундамент паритет-тестов GL↔Vulkan по роадмапу (цель SSIM ≥ 0.999).
  Скрипты: `scripts/golden_capture.sh` (детерминированные кадры
  menu/space/space_noui через SIRIUS_GOLDEN_DIR), `golden_compare.py`
  (оконный SSIM + MAE + diff-карта, поддержка масок), `golden_gate.sh`
  (полный гейт против `tests/goldens/gl/`, порог 0.9985 = измеренный пол
  воспроизводимости GL↔GL; поломка рендера даёт <0.99). Каркас выбора
  бэкенда: `SIRIUS_RENDERER=gl|vulkan` (`RenderBackendSelector`),
  `VKRenderContext` — заглушка до Phase 3. Детерминизм (всё ТОЛЬКО при
  SIRIUS_GOLDEN_DIR, обычная игра не затронута): RenderClock
  (замораживаемый клок презентации: UI-пульсы, курсоры, кольца лейнов,
  спин планет/доков, material-анимации, чат-fade), фиксация intro-сцены
  №0, заморозка THN меню, телепорт в «директорскую позу» на сервере,
  warp мыши + жёсткий размер окна, отключение ambient/частиц/пыли
  поля/маяков. Измерено: menu SSIM=1.00000 (бит-в-бит), space/noui
  ≥0.9995 по трём прогонам. Известное ограничение: зона дока планеты
  (CMP-loop фаза) маскируется `space.mask.png` (<3% кадра) — проверить
  при первом GL↔VK прогоне. Сиды AsteroidField/Nebula временно
  зафиксированы (12345) — вернуть Random() после Phase 3. Попутные
  фиксы вне golden: clamp катастрофического catch-up в
  FixedTimestepLoop (5-секундный столл превращался в один гигантский
  шаг физики — крах BepuPhysics PairCache), guard мёртвого лидера
  формации в FormationBehavior (AccessViolation на удалённом теле).
- **Vulkan-миграция, Phase 3 — сессия 2 (2026-06-10, в процессе)**:
  13 фиксов до почти-паритета: камера-блок 1 (тег-кэш, GL-семантика),
  немедленные сеттеры состояний, Apply()-перед-draw (главный замок —
  стейт материалов не достигал бэкенда), **depth-ремап GL[-1,1]→VK[0,1]**
  (вся 3D-геометрия клипилась по z), **VKMemoryPool** — суб-аллокатор
  64МБ-блоками (лимит 4096 vkAllocateMemory пробивался сценой космоса),
  переполнение stackalloc-дескрипторов (порча стека → отложенный GC-краш),
  unmanaged стрим-scratch (use-after-free pinned при Expand), CPU-расш.
  легаси 16-бит форматов, frame-stamped deferred destroy под локом,
  отдельный fence ридбэка, single-frame семантика (wait-all fences),
  пул acquire-семафоров, снята вся телеметрия. Меню рендерит лого
  Sirius и текст; остаток: растянутые 2D-квады в golden-захвате,
  флаки-сегфолт пути ридбэка (только при покадровом дампе), возврат
  HDR-пути, SSIM-гейт, Vulkan-дефолт.
- **Vulkan-миграция, Phase 3 — bring-up (2026-06-10, в процессе)**:
  собственный raw Vulkan 1.3 бэкенд (`Backends/Vulkan/`): рукописные
  биндинги (function pointers через SDL), dynamic rendering, sync2,
  свопчейн + 2 кадра в полёте, шейдеры из SPIR-V бандлов с патчем
  binding'ов (текстура→2N, сэмплер→2N+1; UBO=block index), дескрипторные
  цепочки пулов, транзиентные стрим-буферы, текстуры (включая BC/DXT и
  кубы) со staging-загрузкой, рендер-таргеты + блиты, скриншоты
  (ReadBackBuffer в IRenderContext). **Главное меню и cubemap-небо
  космоса уже рендерятся живьём на RTX 5090**; ориентация неба совпадает
  с GL (negative-height viewport). Ключевой пойманный класс багов:
  активация шейдера двумя путями (ApplyShader у материалов + diff
  requested.Shader у фронтенда) — оба обязаны обновлять applied.Shader.
  Валидация: LunarG SDK в /tmp/1.4.350.1 (SIRIUS_VK_VALIDATION=1 +
  VK_LAYER_PATH). Отладка: SIRIUS_VK_DUMPFRAMES, SIRIUS_VK_TESTPATTERN.
  Остаток: vbuffer-дро в космосе (планеты/HUD), белая «шторка» в
  golden-режиме (заморозка), возврат HDR-пути (диагностический байпас),
  снятие телеметрии, golden-паритет SSIM≥0.999.
- **Vulkan-миграция, Phase 1 — единый HDR-кадр (2026-06-10)**: сцена
  рендерится в линейный RGBA16F-таргет (`Render/HdrFramePipeline`,
  перегрузка `RenderTarget2D(ctx, texture)`, `SamplerState.LinearClamp`)
  и выходит на экран одним Tonemap-проходом (шейдер `Tonemap` —
  identity до этапа HDR-света). MSAA резолвится в HDR-таргет. Врезка —
  2 вызова в SystemRenderer.Draw, THN-меню покрыто автоматически.
  Гейт: SSIM 0.9993–0.9996 против baseline до изменения — картинка
  не изменилась.
- **Vulkan-миграция, Phase 2 — верификация SPIR-V-канала (2026-06-10)**:
  исправлен баг `ShaderBundleWriter.GetShaderBytes` — для фрагментного
  стейджа записывались БАЙТЫ ВЕРШИННОГО при фрагментной длине (GL-путь
  читает только GLSL и не замечал; Vulkan читает SPIR-V — был бы крах
  на каждом шейдере). Новый постоянный тест `ShaderBundleSpirvTests`:
  парсит все 26 встроенных бандлов в точности как будущий
  Vulkan-бэкенд (zstd → таблица вариантов → shdr-заголовок →
  SDL_GPU-обёртка стейджей с БИАСНЫМ varuint как в SpanReader) и
  проверяет магию SPIR-V обоих стейджей. Грабля сборки: Shader.targets
  вызывает `LLShaderCompiler/bin/Debug` — правки компилятора требуют
  его пересборки в Debug + удаления obj/.../Shaders/*.bin. Гейт после
  перегенерации бандлов: зелёный (menu флапает 0.9985–0.9993 — на
  грани порога, известный пограничный случай).
- **HQ-небо Li01 через Real-ESRGAN (2026-06-10, «путь Б»)**: туманность
  Li01 апскейлена локально 2048→4096/грань. Пайплайн
  (`Output/tools/realesrgan/`): декод DDS → 6 граней с «юбкой» 64px,
  сэмплированной из соседних граней по кубической топологии (ESRGAN
  видит продолжение за ребром — швы не расходятся) → realesrgan-x4plus
  ×4 на GPU → срез юбок, даунскейл 8192→4096 LANCZOS → альфа из
  оригинала (LANCZOS, без галлюцинаций на маске плотности) → крест →
  DDS. Результат: edge-тест 1.6 (идентично оригиналу), три слоя
  грузятся, ошибок 0. Бэкап 2048-версии: /tmp/li01_cubemap_2048_backup.dds.
- **Конвертер: режим `--from-cross`**: CPU-путь (без GL) сборки
  cubemap DDS из 4×3 крест-PNG (`RunFromCross`) — обратная дорога для
  любых внешне улучшенных небес; `--game-data` для него не требуется.
  Вариант ChatGPT (`cubemap_li01_enhanced.png`) отклонён по качеству:
  1448×1086 (грань 362²) вместо ≥8192×6144, без альфы, «шахматка» в
  углах; композиция при этом была соблюдена (сдвиги (0,0) по граням).
- **Небо ездило по вертикали вместе с камерой**: в шейдер уходил инверс
  полного `ViewProjection` (с трансляцией); на координатах масштаба
  Freelancer разность far−near в реконструкции луча теряет точность, и
  позиция камеры просачивается в направление сэмплинга. Фикс
  (`StarsphereCubemapRenderer`): матрица собирается из view **без
  трансляции** (`view.Translation = 0` → `Invert(view*projection)`) —
  канонический приём скайбоксов; позиция камеры теперь физически не
  участвует в выборке неба.

## 2026-06-09 (вечер) — Ambient AI v2 + переговоры пилотов (GCS)

Полное описание: `docs/AI_AND_COMMS.md`.

### Ambient AI v2
- `SAmbientBehaviorComponent` (заменил `SWanderComponent`): поведение по
  `behavior` encounter-зоны — trade (полёт к базе/гейту/трейдлейну и
  стыковка с деспавном), patrol_path (петлевой маршрут на крейсере),
  wander; `EncounterInfo.Behavior` прокинут из `[EncounterFormation]`.
- Формации для групп (кроме торговцев), самороспуск осиротевших ведомых.
- Бегство повреждённых торговцев (<65% хулла, 45 с) с криком в эфир.
- Взаимопомощь: `ServerWorld.RequestAssistance` — патрули той же фракции
  в 7 км вступают в бой при первом контакте союзника.
- Репопуляция системы до бюджета 36 кораблей каждые 40–60 с.

### Переговоры пилотов (GCS chatter)
- `Server/Comms/`: `GcsVoiceLines` (грамматика оригинала: позывной
  faction_short → formationdesig_NN → number → number; `+` = продолжение,
  `-` = терминальный клип), `SChatterComponent` (голос + постоянный
  позывной, кулдаун 18 с), `ChatterDispatcher` (эфир: 1 фраза/5 с,
  слышимость 12 км, ATC-ответы голосом `atc_leg_m01/f01`).
- События: запрос дока + ответ ATC, первый контакт (enemysighted /
  order_engage у лидера звена), получение/нанесение урона, бегство,
  предсмертный крик, all-clear через 8 с тишины.
- RPC `IClientPlayer.Chatter(NetDlgLine[])`; клиент проигрывает
  последовательность клипов через `PlayVoiceLine` (`RunChatter`).
- Фраза собирается только из клипов, реально существующих у голоса;
  иначе NPC молчит — неверные последовательности исключены.

### Тесты
- `LibreLancer.Tests/GcsChatterTests.cs` — 7 тестов грамматики; весь
  набор 269/269 passed.
- `Output/verify_gcs_sequences.py` — сверка с данными Discovery:
  259 пар «фракция×голос», боевые фразы 100%, dock request 223/259
  (36 голосов без клипа имени фракции корректно молчат).
- `Output/verify_utf_clips.py` — проверка уровня аудиофайлов: FLHash
  каждого сегмента найден как узел в voice-паке `.utf` —
  18 952 клипа, 0 отсутствующих.
- Runtime (headless-прогон, SIRIUS_AUTOPLAY): живой геймплей Li01 —
  торговец `co_vr` произнёс запрос дока: `faction_short →
  formationdesig_04 → number_4 → number_5 → dockrequest_request_02+`,
  клиент проиграл цепочку 5/5 клипов за ~3 с, «clip not found» — 0.
- Фикс по результатам прогона: ответ ATC обходит глобальный кулдаун
  эфира (это продолжение начатого обмена), иначе granted-реплика
  отсекалась 5-секундным интервалом.

### Автотест-режим
- `SIRIUS_AUTOPLAY=1`: меню → новая игра → авто-взлёт в космос; первый
  репоп-тик форсирует один dock-request у ближайшего NPC (проверка
  всего пути фразы в headless CI). См. `docs/AI_AND_COMMS.md` §5.

## 2026-06-09

### Discovery data loading
- Added parsing for Discovery `WeaponModDB = equipment\\WeaponModDB.ini` from `[Data]` in `EXE/freelancer.ini`.
- Added fallback loading for `DATA/EQUIPMENT/weaponmoddb.ini` when the key is absent.
- Added parser for `commodities_per_faction.ini`:
  - `[FactionGood]`
  - `faction = ...`
  - multiline `MarketGood = good, min, max`
- Added fallback loading for `DATA/EQUIPMENT/commodities_per_faction.ini`, because Discovery 4.86 ships the file but does not reference it from `[Data]`.
- Added runtime `FactionCommodityProfile` storage and linked profiles to `Faction` plus `BaseSoldGood` records.
- Added Discovery-safe `librelancer.ini` for the Discovery root. It has an empty `[Resources]` section and direct `[Data]` paths, so Linux runs do not attempt to load `Discovery.dll`, `DsyAddition.dll`, or other Windows DLL resources.

### Weapon/shield damage compatibility
- Added runtime shield modifier storage to `MunitionEquip` and `MissileEquip`.
- Applied `WeaponModDB` shield modifiers during equipment initialization.
- Integrated shield damage modifiers into server projectile damage.
- Integrated shield damage modifiers into missile explosion damage per affected target.

### Discovery-specific equipment recognition
- Added `DiscoverySpecialEquipmentKind` with `JumpDrive`, `DockingModule`, and `SurveyModule`.
- Classified Discovery jump drives, survey modules, and docking modules in `ShieldEquipment` based on their Discovery nicknames. Discovery encodes these items as `[ShieldGenerator]` entries in `st_equip.ini`.

### Fuse compatibility
- Added parser support for Discovery/large-ship fuse child sections:
  - `[dump_cargo]`
  - `[damage_root]`
  - `[damage_group]`
  - `[tumble]`
- Added `particles` alias support for `[start_effect]` and made runtime fuse effect spawning skip empty effect names safely.

### Additional schema compatibility
- Added Discovery/vanilla-extended fields found during the audit:
  - `CollisionGroup.explosion_resistance`
  - `Armor.category`
  - `Munition.owner_safe_time`
  - `NewCharPilot.thumb`
  - `LightSource.color_curve`
  - `SystemInfo.name`
  - `EncounterFormation.longevity`
  - `Asteroid.explosion_impulse`
  - `Asteroid.phantom_physics`
  - `DynamicAsteroid.particle_effect`
  - `Simple.MinSpecLOD`
- Added shiparch compatibility fields/handlers used by Discovery ship/solar-like ship entries:
  - `solar_radius`
  - `destructible`
  - `shape_name`
  - `loadout`
  - `distance_render`
  - `docking_camera`
  - multiline `docking_sphere`

### Audit result
- Audited Discovery `EXE/freelancer.ini`: 97 `[Data]` entries.
- Audited main referenced data groups and dependent universe graph files.
- After the changes, the audit reports no unknown sections or entries in the covered Discovery schema groups.

### Not completed in this pass
- Full FLHook replacement gameplay systems are not implemented here:
  - player-owned bases,
  - full jump-drive activation/gameplay,
  - full cloak detection/energy gameplay,
  - player slash commands,
  - mine control,
  - complete dynamic faction economy simulation.
- The changes add parser/runtime foundations and damage/economy data wiring needed before those gameplay systems can be implemented safely.
